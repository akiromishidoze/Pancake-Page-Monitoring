services:
  app:
    build: .
    ports:
      - "3001:3001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backups:/app/backups
    environment:
      - DATABASE_URL=postgresql://user:password@host.docker.internal:5432/botcake_monitor
      - PORT=3001
      - FB_ACCESS_TOKEN=${FB_ACCESS_TOKEN:-}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-}
    restart: unless-stopped
